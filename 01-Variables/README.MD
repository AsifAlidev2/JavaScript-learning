# Variables in JavaScript

## 📌 Description
This section explains the concept of variables in JavaScript. Variables are used to store data values that can be reused and modified during program execution.

## 🧠 Topics Covered
- `var` keyword
- `let` keyword
- `const` keyword
- Basic data types (Number, String, Boolean)

## 📝 Explanation
- **var**: Old way to declare variables (function scoped).
- **let**: Modern way to declare variables (block scoped).
- **const**: Used to declare variables whose value cannot be reassigned.

## 💻 Example Code
```javascript
let name = "Asif";
let age = 22;
const isStudent = true;

console.log(name);
console.log(age);
console.log(isStudent);
