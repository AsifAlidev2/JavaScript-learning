# JavaScript Functions & Array Methods

This repository contains a well-structured JavaScript file that demonstrates **core concepts of functions and array methods**.  
It is created for **learning, practice, and revision purposes**, especially for beginners.

---

## 📌 Topics Covered

### 1. Functions
- Function declaration & definition
- User-defined functions
- Difference between **Function** and **Method**
- Arrow functions (ES6)

### 2. Array Methods
The following built-in JavaScript array methods are explained with examples:
- `push()` – Add element at the end
- `pop()` – Remove last element
- `shift()` – Remove first element
- `unshift()` – Add element at the beginning
- `slice()` – Extract part of array (without modifying original)
- `splice()` – Add or remove elements (modifies original array)

---

## 📂 Project Structure

